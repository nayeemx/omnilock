name: Build OmniLock

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache (app)
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Rust cache (service)
        uses: swatinem/rust-cache@v2
        with:
          workspaces: service

      - name: Install dependencies
        run: npm ci

      - name: Build service daemon
        run: |
          cargo build --release --manifest-path service/Cargo.toml
          mkdir -p src-tauri/resources
          copy service\target\release\omnilock-svc.exe src-tauri\resources\
          copy service\target\release\omnilock-monitor.exe src-tauri\resources\

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: OmniLock-installer
          path: src-tauri/target/release/bundle/nsis/OmniLock_*_x64-setup.exe
